---
import ProjectLayout from '@/layouts/ProjectLayout.astro';
import { projects } from '@/data/projects';
const { title, img, demoUrl, description } = projects.fitness;
---

<ProjectLayout
  title={title}
  img={img}
  description={description}
  demoUrl={demoUrl}
/>
