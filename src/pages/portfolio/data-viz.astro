---
import ProjectLayout from '@/layouts/ProjectLayout.astro';
import { projects } from '@/data/projects';
const { title, img, demoUrl, description } = projects.dataViz;
---

<ProjectLayout
  title={title}
  img={img}
  description={description}
  demoUrl={demoUrl}
/>
